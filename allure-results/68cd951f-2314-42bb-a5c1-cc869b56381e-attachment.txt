// Generate a random number greater than 12
let invalidResourceId = Math.floor(Math.random() * 10) + 13;

// Set it as an environment variable
pm.environment.set("invalid_resource_id", invalidResourceId);
//Status code check
pm.test("Status is 204 No Content", () => {
    console.log("Status Code:", pm.response.code);
    pm.response.to.have.status(204);
});

//Body is empty check
pm.test("Body is empty", () => {
    let bodyText = pm.response.text();
    console.log("Response Body (should be empty):", bodyText);
    pm.expect(bodyText).to.eql('');
});


//Now unset all the variables:
// List of variable names to unset
let vars = [
    "userId",
    "token",
    "resource_id",
    "user_id",
    "page_number",
    "invalid_resource_id",
    "invalid_page_number",
    "user_id_plus_12"
];

// Unset from environment
vars.forEach(v => pm.environment.unset(v));


